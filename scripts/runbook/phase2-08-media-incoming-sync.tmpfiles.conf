# /run is tmpfs, wiped every boot -- this recreates the trigger
# directory on every boot before anything can write to it.
# 2775 + setgid so files created inside (by radarr or sonarr, both in
# "media") inherit the "media" group, and the group can write/delete.
d /run/media-incoming-sync 2775 root media -
